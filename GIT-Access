The libaosd GIT repository can be checked out using the following command:
	git clone git://git.atheme.org/libaosd.git libaosd

libaosd's GIT repository depot can be browsed over the internet at 
the following address:
	http://git.atheme.org/
